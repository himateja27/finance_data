# Environment Configuration Template

# Django Settings
DEBUG=True
SECRET_KEY=django-insecure-your-secret-key-change-in-production-12345
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
DB_NAME=finance_db
DB_USER=finance_user
DB_PASSWORD=finance123
DB_HOST=localhost
DB_PORT=3306

# CORS Configuration (for frontend integration)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5174,http://127.0.0.1:3000

# Optional: Token expiration (in seconds, None = no expiration)
# TOKEN_EXPIRATION_SECONDS=86400
